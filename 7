ing UnityEditor.Experimental.GraphView;
using UnityEditor.Search;
using UnityEngine;
using UnityEngine.LowLevelPhysics2D;

public class GameManager : MonoBehaviour
{
    bool tienepocion;
    bool estaenzonasegura;
    bool curaciondisponible;
    bool estamuerto;


    // Start is called once before the first execution of Update after the MonoBehaviour is created
    // ejercicio del 1 al 15
    void Start()
    {

        if (tienepocion = true) curaciondisponible = true;
        else (tienepocion= false) curaciondisponible = false;  

        if (estaenzonasegura = true) curaciondisponible = true;
        else (estaenzonasegura=false) curaciondisponible = false;

        if (estamuerto = true) curaciondisponible = false;


    

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
